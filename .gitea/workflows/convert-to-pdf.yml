name: PDF

on:
  workflow_dispatch:
  push:
    branches:
      - resume
    paths:
      - '.gitea/workflows/convert-to-pdf.yml'
      - 'README.md'

jobs:
  pdf:
    name: Build PDF
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: PDF Generation
        uses: DrMaxNix/markdown-to-pdf@v1
        with:
          input_dir: ./README.md
          output_dir: ./
          build_html: false
      - name: Rename PDF
        run: mv README.pdf leonardo_amaral_resume.pdf
      - name: Release
        uses: "softprops/action-gh-release@v2"
        with:
          repo_token: "${{ secrets.GITEA_TOKEN }}"
          make_latest: true
          prerelease: false
          name: "Curriculo em PDF"
          files: leonardo_amaral_resume.pdf

